# OS-Deskew: 输出侧固定滞后平滑的全分辨率 Ego-Motion 补偿
### (Output-Side Fixed-Lag Smoothed Deskewing on Point-LIO)

**一句话主张:现有 LIO 的二次去畸变全部服务于"配准/位姿精度"(估计侧、帧级);
本工作首次把去畸变作为"面向下游感知的输出产品"来做——用固定滞后平滑器精化
Point-LIO 的逐点状态流,对全分辨率点云做第二遍去畸变,并用下游无人机检测
指标闭环验证补偿质量。**

基于 Point-LIO fork 改造(<https://github.com/hku-mars/Point-LIO>)。
环境:Ubuntu / ROS2 Jazzy(原计划 ROS1 Noetic,实际在此仓库上以 ROS2 实现);
传感器 Livox Mid-360;部署目标 Jetson Orin NX 16GB。
下游应用:近场(≤3.5m)蜂群邻机检测(单帧目标仅 5-15 点)。

---

## 状态(2026-07-27)

**核心场景是零延迟避障,用的是 C1(`/nearfield/deskewed_*`),不是 C2。** C2 已经做完
放在那,不是当前优先级,先别默认往这个方向继续投入。

| 项 | 状态 | 备注 |
|---|---|---|
| C1 解耦全分辨率补偿 | ✅ 已实现、编译通过、已验证 | 见下方"C1 实现细节";**这是当前场景实际要用的那条路径** |
| §4 先导实验(C2 go/no-go) | ✅ 已跑,结论 **GO** | 用的是替代数据,不是 §7 规范的数据① |
| C2 固定滞后 RTS 平滑器 | ✅ 已实现、编译通过、已验证 | 见下方"C2 实现细节";v1 版本,简化的两遍(前向KF+后向RTS)近似,非严格 IKFoM 联合 RTS。**已确认对当前避障场景不适用(引入延迟),按你的决定暂缓** |
| C2 验证用的诊断代码(状态记录器、修正幅度诊断字段) | 🗑️ 已从代码里移除 | 只在验证时临时加回去过一次(lag 对比实验),用完即删,不留在仓库里;见下方"已移除的实验脚手架" |
| 实验二:建图对比(新增,见下方"验证计划") | ❌ 未开始 | 需要你录两段新数据(空场景静态参考 + 同场景运动),是回答"C2 是否真的更准"的正确方法,目前还没做过 |
| C3 精度需求分析 | ❌ 未开始 | |
| B1(FAST-LIO2)/ B2'(DLIO)/ AC-LIO 基线 | ❌ 未开始 | |
| 数据①(手持晃动+静止无人机,分档协议) | ❌ 未录制 | 需要你物理操作录制,见下方"数据现状" |
| 数据②③④ | ❌ 未确认/未录制 | 见下方"数据现状" |
| 下游检测闭环评估 | ❌ 未开始 | 依赖 `ICASSP-LiDAR-Drone-Detect` pipeline(另一个 repo,`SILENT_detection/ICASSP-LiDAR-Drone-Detect`);2026-07-27 清掉了那边的旧方案(`src/lidar_drone_detect/`、`scripts/run_pipeline.py`、`docs/README_swarm_lio_realtime_pipeline.md`、`out/` 诊断产物),保留 `src/silent_detect` |

---

## 1. 问题与动机

### 1.1 Ego motion distortion

LiDAR 一帧 = 0.1s 积分窗口,窗口内每个点在不同时刻、不同传感器位姿下采集,
打包后却被当作同一时刻采集 → 静态结构被拉歪。观测者运动越激进畸变越大,
旋转项主导且被距离放大(位置误差 ≈ r·δθ:姿态残差 1° 在 6m 处 = 10.5cm)。

### 1.2 为什么现有方案对我们不够

下游是**近场稀疏目标检测**(5-15 点/目标),对输出点云质量的要求
远高于对里程计 ATE 的要求,且有两个硬约束:
- **全分辨率**:任何降采样都可能抹掉目标 → Point-LIO 的实时抽稀
  (point_filter_num>1)不可接受地作用在需要的点上
- **Jetson 实时**:全局 point_filter_num=1 延迟不可行(已实测)

而整个 LIO 领域的去畸变都是**估计导向**的:去畸变是配准的前处理,
质量以位姿精度衡量,作用在参与配准的抽稀点上。**输出给下游的点云质量
从来不是一等公民。**

---

## 2. 相关工作全景(= 论文 Related Work 骨架)

按"初始去畸变 / 二次去畸变 / 粒度 / 服务对象"四轴分类:

| 方法 | 初始去畸变 | 二次(further)去畸变 | 粒度 | 服务对象 |
|---|---|---|---|---|
| LOAM / F-LOAM | 匀速假设 | 收敛后重投影 | 帧 | 估计 |
| FAST-LIO2 | IMU 反向传播 | ✗ | 帧 | 估计 |
| DLIO (ICRA'23) | IMU 积分(粗) | 解析连续轨迹(细) | 帧 | 估计 |
| CT 家族: CLINS / SLICT / SLICT2 / Coco-LIC / Traj-LO / CT-ICP | — | 连续时间联合优化 | 连续 | 估计 |
| AC-LIO (2024) | IMU | IESKF 迭代内渐进重去畸变 + 收敛准则门控 | 帧×迭代 | 估计 |
| ADC-LIO (IJRA'25) | IMU | 迭代 Kalman 平滑 + 反向传播;自适应畸变协方差 | 帧×迭代 | 估计 |
| Point-LIO (2023) | 无需(逐点即时) | ✗(纯因果,无回溯) | 点 | 估计 |
| Vibration-aware LIO (2025) | IMU | ✗(建模残余不确定性,只加权不修正) | 点 | 估计 |
| **本工作** | Point-LIO 逐点(因果) | **固定滞后平滑 → 全分辨率二次去畸变** | **点** | **输出/下游感知** |

要点(写 related work 时直接用):
- 匀速/帧级假设在激进运动下失效 → Point-LIO 的动机
- CT 家族的死穴(AC-LIO 指出):高速动态下无约束轨迹不一定符合预定义
  模型(时间分辨率受限),且连续时间优化计算代价高
- DLIO 的局限(AC-LIO 指出):受 IMU 固有精度限制,且后端配准中不再
  进一步去畸变
- AC-LIO/ADC-LIO 已经证明"平滑/迭代式二次去畸变"有效——**但都在
  估计回路内、帧级、服务配准**;"平滑思想"本身不是我们的 novelty
- Vibration-aware LIO 承认"去畸变后仍有残差"并为其建模不确定性——
  从侧面论证了残差真实存在,我们选择修正而非仅加权
- Point-LIO 的因果滞后:处理第 k 点时状态只含前 k-1 点信息;
  该点之后 0.1s 内的观测本可修正对该时刻的估计但未被利用
  (经典的 filtering vs smoothing 差异)

### 我们不 claim 什么(诚实边界,防审稿人)

- ✗ 不 claim "首次迭代/平滑式去畸变"(AC-LIO/ADC-LIO 在先)
- ✗ 不 claim 里程计精度提升:精化输出**不反馈**进配准回路,
  ATE 与 Point-LIO 原生持平(这是特性不是缺陷:估计器稳定性零风险)
- ✓ claim 的是组合:**逐点粒度 × 输出侧 × 全分辨率 × 延迟-精度旋钮 ×
  下游检测闭环评估**。每一轴单独存在,组合没人做过

---

## 3. 三个贡献

### C1:解耦全分辨率补偿(系统贡献)—— ✅ 已实现

状态估计路径(抽稀,保实时)与去畸变输出路径(全分辨率)解耦。
借 Point-LIO 逐点状态"免费"把点级补偿覆盖到 100% 的点
(原生 Point-LIO 中 50-75% 的点被抽稀,从未享受逐点补偿)。

### C2:输出侧固定滞后平滑二次去畸变(方法核心)—— ✅ v1 已实现

对 Point-LIO 的逐点状态流跑固定滞后平滑(fixed-lag smoother),
用平滑轨迹对滞后窗口内的全分辨率点做第二遍变换,消除因果滞后残差。
双档输出:低延迟因果版(`/nearfield/deskewed_*`,C1)+ 高精度平滑版
(`/nearfield/refined_*`,C2,延迟=lag),下游按需选。

### C3:面向下游的补偿精度分析与闭环评估(评估贡献)—— ❌ 未实现

- 推导 δp ≈ r·δθ 的距离放大律,从近场检测需求(中心误差<0.1m @3.5m
  → δθ<1.6°)反推补偿精度等级
- **用下游检测指标(recall/RMSE)评估去畸变质量**——领域内首个;
  其他 LIO 论文只有 ATE,我们独有下游证据链

---

## C1 实现细节(已完成)

代码全部在这个 fork 里,`nearfield.enable: false` 时零行为改变(默认关闭)。

```
/livox/lidar (全分辨率 CustomMsg)
   ├─→ point_filter_num 抽稀 → Point-LIO EKF 逐点更新 → 状态流(原有逻辑,不动)
   └─→ 近场旁路 blind_override < r ≤ near_range,零抽稀
              └─→ 逐点用"当时"状态变换 → 三路输出(不参与 EKF 更新)
```

**改动文件**:
- `src/preprocess.h` / `src/preprocess.cpp`:`avia_handler` 里新增 `pl_nearfield`
  容器,近场点(`nearfield_blind_override < r ≤ nearfield_near_range`)无条件
  收进去,不受 `point_filter_num` 抽稀影响;`nearfield_join_update`(默认 false)
  控制是否也走原逻辑进 `pl_surf` 参与 EKF 更新
- `include/common_lib.h`:`MeasureGroup` 新增 `nearfield` 字段,与 `lidar` 同步
- `src/laserMapping.cpp`:
  - `nearfield_buffer` deque 与 `lidar_buffer`/`time_buffer` 严格索引对齐
    (`standard_pcl_cbk` 非 Livox 路径也同步 push 空云,避免错位)
  - 主循环每个 `time_seq` 组 EKF 更新成功后立即 `flush_nearfield_upto(time_current)`,
    复用现有 `pointBodyToWorld()`——近场点**完全不进** `update_iterated_dyn_share_modified`
  - 三路输出:
    - `/nearfield/raw_body`:未处理原始点(A/B 对照组)
    - `/nearfield/deskewed_world`:逐点用当时状态变换到 world frame
    - `/nearfield/deskewed_body`:把 `deskewed_world` 用**帧末最终状态**反投影回
      body frame,让整帧点统一到同一参考时刻
  - 输出字段:`x,y,z,time(帧内相对秒),range(原始距离),intensity`
  - 新增 `log_state_if_due()` / `StateLogRecord`:每次成功的 EKF 更新后,
    以 `state_log.hz`(默认 1000Hz)子采样记录 `{t, q, p, v, cov_pos对角, cov_rot对角}`
    到二进制文件,为 C2 的平滑器和先导实验做准备
- `src/parameters.h` / `src/parameters.cpp`:新增 `nearfield.*`、`state_log.*` 参数
- `config/mid360.yaml`:新增 `nearfield:` 与 `state_log:` 配置块,均默认关闭

**不要做的事(已遵守)**:
- 近场点不降采样、不体素滤波(`filter_size_surf`/`filter_size_map` 不作用于这条路径)
- 不改动原有状态估计逻辑,近场路径是纯旁路

**回归验证**(`1m_move_side_1` bag,`nearfield.enable=false` vs `true` 背靠背跑):
- 轨迹一致性:`/aft_mapped_to_init` 按时间戳对齐后,位置差 mean=2.7mm, max=7.8mm
  → 近场旁路未污染状态估计
- 密度:`/nearfield/deskewed_body` 零降采样点数远超 baseline `/cloud_registered`
  (后者叠加了 point_filter_num 抽稀 + 体素滤波两层压缩),符合预期
- A/B 去畸变效果:快速运动帧下 raw vs deskewed 逐点平均偏移 0.6m,最大 1.6m

---

## §4 先导实验(C2 go/no-go)结果 —— ✅ GO

**用途**:C2(完整 RTS 平滑器)投入前先量化收益上限,避免白做。

**v1 近似方法**(按文档"第一版可用未来Δ秒的因果估计插值回来"):对状态流做
前向 Δ=0.1s 窗口、**逆协方差加权平均**(位置直接加权平均;姿态在因果估计的
切空间里加权平均再指数映射回去)。**这不是完整 RTS,只是收益上限的粗略估计。**

**数据**:`1m_move_side_1`(非正式的替代数据,不是 §7 规范的分档协议数据①,
但角速度天然覆盖了低/中/高三档,足够做 go/no-go)。

**结果**(按 IMU 角速度分档,4275 个近场点,每帧均匀子采样 30 点):

| 角速度档 (rad/s) | 采样点数 | mean \|Δp\| | p95 \|Δp\| | max \|Δp\| |
|---|---|---|---|---|
| [0, 0.5) 慢速 | 1793 | 1.0cm | 3.7cm | 14.1cm |
| [0.5, 2.0) 中速 | 1912 | 6.5cm | 21.0cm | 46.3cm |
| [2.0, ∞) 快速 | 570 | **18.4cm** | 47.3cm | 78.0cm |

**判据**(按原计划):快速档 mean 18.4cm ≥ 厘米级 → **C2 有肉,值得全量实现**。
三档单调递增,与 δp≈r·δθ 的物理预期吻合。

分析脚本:`~/pilot_experiment.py`(不在此仓库内,是离线分析用的一次性脚本)。

**实现中踩的坑**(供以后类似分析参考):
- `sensor_msgs_py.point_cloud2.read_points` 返回的字段是消息声明的原始 dtype——
  这里是 **float32**。把 float32 的帧内相对时间直接加到 ~1.78e9 的 Unix 时间戳上,
  numpy 会按"值不升精度"规则整体退化成 float32 运算,~200 级精度损失直接吃掉
  所有亚秒信息,导致同一帧内所有点的时间戳被错误地合并成几乎同一个值。
  **必须显式 `.astype(np.float64)` 后再和大数相加。**
- 用去畸变点云自身的时间戳范围反推整帧起始时间不可靠:近场点是全帧的
  空间子集,其时间戳跨度可能显著小于真实的 ~0.1s 整帧扫描窗口
  (取决于附近结构在扫描周期内的哪个角度区间被覆盖到)。应使用经验确认的
  整帧时长常量,而不是从子集自身的 min/max 反推。

---

## C2 实现细节(v1 已完成)

**设计**:不逐字实现 IKFoM 状态空间的联合 RTS(需要在每一步暴露内部过程
Jacobian,侵入性太强),而是把 Point-LIO 已经产出的因果状态流本身当作
"一个平滑轨迹的带噪观测序列",在这份 (t, R, p, v) 流上跑一个轻量的两遍
(前向 Kalman + 后向 RTS)平滑器:

- **位置**:x/y/z 三轴独立,常速度模型(状态 = [位置,速度]),同时用因果
  流的 position 和 velocity 作为观测(两者都已经是高质量估计,直接用)
- **旋转**:以窗口第一个节点的旋转为参考,把每个节点的旋转差异映射到切
  空间(`Log(R_ref^T · R_i)`),对 3 个切空间分量各自跑随机游走模型的
  KF+RTS,平滑后再通过 `Exp` 映射重建旋转矩阵
- 过程噪声/观测噪声当前是**固定常数**(`os_deskew.q_pos_accel` / `r_pos` /
  `r_vel` / `q_rot` / `r_rot`),**没有**用因果滤波器自己报告的协方差
  (`cov_pos`/`cov_rot`)作为观测噪声——EKF 报告的协方差是"给定模型正确"
  的条件置信度,不一定等于"和一个信息更充分的未来估计相比差多少",直接
  拿来用有虚假过度自信的风险。这是当前实现最大的简化,是后续可以精化的
  方向(比如用实测的因果-平滑残差反过来标定这几个噪声参数)

**代码**:`src/FixedLagSmoother.hpp`(`PoseFixedLagSmoother` 类,独立于
Point-LIO 主循环,先做了一个不依赖 ROS/PCL 的独立编译单测,确认在无噪声
输入下能精确重建轨迹,再接入主程序)。

**在线运行方式**(`src/laserMapping.cpp`):
- `feed_smoother_if_due()`:和 `log_state_if_due()`(§4 用的状态记录器)
  在同一处调用,以同样的 `state_log.hz` 子采样把因果状态喂给平滑器
- `flush_nearfield_upto()` 额外把每个近场点连同它自己的**因果世界系变换
  结果**(`PendingNearfieldPoint::p_world_causal`)一起存进待处理队列——
  这一点很关键:refined 输出的点是"攒够了未来数据才发布"的,批次边界和
  C1 的逐帧发布对不上,如果不在源头把同一个点的因果变换存起来,之后单靠
  已发布话题的点顺序去配对因果 vs. refined 会配错点(见下方"踩的坑")
- `publish_refined_if_ready()`:每帧调用一次,把 `pose_smoother.latest_time()
  - lag` 之前的点全部弹出、跑一次 `run_smoothing_pass()`(整个窗口重算一次,
  不是每个点重算)、插值、变换、发布,然后裁剪滑动窗口(`trim_before`)
- 输出:`/nearfield/refined_body`、`/nearfield/refined_world`,延迟固定
  = `os_deskew.lag`(默认 0.1s)

**验证**(`1m_move_side_1`,`nearfield.enable=true` + `os_deskew.enable=true`):
- 延迟:最后一条 `refined` 消息时间戳比最后一条因果 `deskewed` 消息晚
  **正好 0.100s**,与配置的 `lag: 0.1` 精确吻合
- 主轨迹回归:`/aft_mapped_to_init` 对比纯 baseline,位置差 mean=2.65mm
  max=7.84mm——和 C1-only 的回归结果(2.7mm/7.8mm)基本一致,确认 C2
  同样没有污染状态估计(结构上也保证了这一点:C2 从不写 `kf_output`/
  `kf_input`,只读)
- refined vs 因果的修正幅度(**同一个点**直接相减,见"踩的坑"):
  整体 mean=5.1cm, p95=5.8cm, max=10.1cm;按角速度分档 mean 从慢速 5.08cm
  到快速 5.30cm,只有很轻微的上升趋势——比 §4 先导实验用的 v1 近似
  (1.0cm → 6.5cm → 18.4cm,随角速度显著上升)平坦得多

**这个"平坦"结果怎么解读**:不是说 C2 没做事(10cm 级修正是真实存在的),
而是当前固定噪声参数的调法本身对运动强度不敏感——先导实验的 v1 近似是
"越剧烈运动窗口内因果估计的方差本身越大,朴素加权平均自然被拉得更远",
而真正的 RTS 用固定 Q/R,平滑强度基本不随窗口内数据的实际离散程度自适应。
**这不是 bug,是这一版参数选择的真实局限**,写在这里防止后续拿这个数字
直接当"C2 效果如先导实验预测"来用——两者的物理含义不同,不能直接对比。
调参(或者改用因果协方差做观测噪声,并想办法验证它没有虚假过度自信)
留给下一步。

**踩的坑**:第一次验证时直接拿"时间最接近的因果消息"去和 refined 消息
按点序配对,算出 mean=0.66m、max=6.9m 的离谱结果,且不随角速度变化——
后来意识到 refined 批次是按"攒够未来数据"发布的,和因果输出的逐帧批次
边界对不上,配对的其实是两个不同时刻、不相关的点。修法见上("在源头把
同一个点的因果变换存起来"),不是靠事后从录制的 bag 里再配对。

**lag 扫描**(0.05s vs 0.1s,同一份数据,2026-07-27):

| lag | 整体 mean | 整体 p95 | 整体 max |
|---|---|---|---|
| 0.05s | 5.13cm | 5.75cm | 9.08cm |
| 0.10s | 5.11cm | 5.78cm | 10.14cm |

lag 减半,修正幅度几乎没变(ratio=1.01)。按直觉,lag 越短平滑器能看到的
"未来"数据越少,修正应该跟着变小——没观察到这个趋势,说明当前修正幅度
主要是被固定的 `q_pos_accel`/`r_pos`/`r_vel`/`q_rot`/`r_rot` 这几个常数
决定的,对"到底能看多远的未来"不敏感。这和上面"平坦"结果是同一个调参
局限的两个侧面,不是独立新发现。**这个数字回答的是"修正前后差多少",
不是"更准了没有"**——测这个的正确方法是下面"验证计划"里的实验二,还没做。

**已移除的实验脚手架**:为了做 §4 先导实验和上面这些验证,曾经加过两块
代码:(1) 逐点状态记录器(`state_log.*` yaml 开关、`StateLogRecord`、
`log_state_if_due()`),用于离线跑先导实验;(2) 在 `publish_refined_if_ready()`
里借用 `refined_*` 话题本来没用的 "time" 字段,存 refined vs 因果的修正
幅度(靠 `PendingNearfieldPoint::p_world_causal` 让每个点在源头就带着自己
的因果版本坐标,避免"踩的坑"里说的批次错配问题),用于上面这些验证。
两块都验证完之后按要求从代码里删掉了,不在当前 commit 里——上面这些
数字是删除前跑出来的记录,不是可以直接从当前代码复现的产物。lag 扫描
是在临时恢复第(2)块之后跑的,跑完又删掉了。如果以后还要做类似的
逐点对比验证,得重新加回去(逻辑不复杂,照着这段描述抄一遍就行,别去
git log 里翻旧 diff)。

---

## 验证计划(新增,2026-07-27)

你之前问"如何确定 C2 真的在纠正",这个问题当时没有好答案——我们只证明了
"修正前后不一样"和"没有污染主轨迹",从没证明过"修正后更接近真实结构"。
你后来给的这份计划把这个补上了,分两个实验:

| 实验 | 回答的问题 | 需要 GT 吗 | 状态 |
|---|---|---|---|
| 实验一:Pilot 实验 | causal 和 smoothed 差多少?值不值得做? | 不需要 | ✅ 已做(就是上面 §4 + lag 扫描) |
| 实验二:建图对比实验 | smoothed 去畸变后是否真的更接近真实结构? | 用静态场景重建近似 GT | ❌ 未做 |

实验二的核心思路:录一段场景全静止(LiDAR 也不动)的 bag 直接叠加当参考
地图(不用跑 SLAM,没有位姿变化,跑估计反而引入噪声);再录一段同场景、
LiDAR 运动(搭载无人机或手持模拟)的 bag,跑出 causal 和 smoothed 两个
重建,分别 ICP 配准到参考地图坐标系,比较 point-to-plane 距离 / RMSE /
局部平面拟合残差——哪个重建更贴近参考地图,才是"真的更准"的证据。

这个需要你去录两段新数据,录制部分我做不了;ICP 配准、量化指标、
可视化叠加这些我可以做。

---

## 数据现状(截至 2026-07-24)

`~/SILENT_detection/jetson_rosbags/` 下已有的录制**命名不可信**——用实际 IMU
角速度核对后发现:

- `3m_front_static_20260713_035208`、`3m_static_front_1` 虽然叫"static",
  实测角速度很大(mean 0.95~3.6 rad/s,max 达 11-12 rad/s)——观测者其实在动
- `3m_move_front_1/2` 虽然叫"move",角速度几乎为零(max ~0.02-0.1 rad/s)——
  命名和实际运动状态是反的

真正核实过、可用的:
- **数据③候选(全静止/噪声底线)**:`1m_front_static_1/2`、`2m_front_static_1`
  —— 角速度全程 <0.02 rad/s,确认真静止,可直接拿来做噪声底线标定(§6.1)
- **数据①候选(有真实快速运动)**:`1m_move_side_1/2`、`2m_move_front_1` ——
  mean 0.9-1.5 rad/s,max 6-8.5 rad/s,足够支撑先导实验,但**不是**按 §7
  规范(30s静止→1min缓慢→1min快走转身→1min剧烈,档间静止3s)录制的

**尚未存在,需要你物理录制**:
- 数据①的正式分档版本(§7 规范,近场 2-3m 内放静止无人机)
- 数据②(机载走走停停,真实螺旋桨振动谱)
- 数据④(MMAUD)的可用性未确认

---

## 5. 方法实现规格

### 5.1 C1 —— ✅ 已完成,见上方"C1 实现细节"

原计划里的"距离自适应抽稀(主路径,r<8m→2, 8-15m→2, 15-30m→4, >30m→8,
用累积计数器实现)"**未实现**——当前主路径抽稀仍是原生 Point-LIO 的固定
`point_filter_num`,近场旁路本身已经解决了近场密度问题,远场自适应抽稀
是独立的优化项,按需再做。

### 5.2 C2 —— ✅ v1 已实现,见上方"C2 实现细节"

```
状态流(子采样至 state_log.hz,默认 1kHz;feed_smoother_if_due 已实现)
   → 固定滞后 RTS 平滑(滞后 L,默认 0.1s,可配;当前只跑过 L=0.1)
   → 平滑轨迹插值(平移线性 / 旋转的切空间线性插值,非 SLERP,见下)
   → 对滞后窗口内全分辨率点第二遍变换
   → /nearfield/refined_body  (延迟 = L)
   → /nearfield/refined_world (验证叠加)
```

设计要点(已实现部分标注):
- **纯附加层**:不改 Point-LIO 前向滤波,不反馈进配准;整体可开关 —— ✅
  结构上保证(C2 代码只读 kf_output/kf_input,从不写入),回归测试确认
  轨迹差 2.65mm,和无 C2 时一致
- **双档输出是特性**:控制/避障用 causal(零额外延迟,`/nearfield/deskewed_*`),
  检测/建图用 refined(高精度,`/nearfield/refined_*`)—— ✅ 已实现
- 平滑器轻量化 —— ✅ 已实现,但简化程度比原计划更大:不是"状态流子采样
  到 1-4kHz 节点做 RTS,点变换时插值"这个通用描述,而是把整个平滑问题
  简化成因果流上的常速度/随机游走两遍滤波(细节见"C2 实现细节")
- 复杂度:O(节点数) 每窗——✅ 已实测,`1m_move_side_1`(14.6s,~140 帧,
  ~2700 近场点/帧)全程无卡顿,具体延迟数字(x86 vs Jetson)未做专门测量
- **未按计划实现的部分**:旋转插值用的是切空间线性插值,不是 SLERP
  (对小角度、短窗口足够,但和"旋转 SLERP"字面要求不同,记录在此)
- lag 扫描 {0.1,0.2,0.5,1.0}s(E1)未做,只验证过默认的 0.1s

### 5.3 C3(未开始)

- 数据①实测:静止目标质心抖动 & 平面厚度 vs 角速度档 → 拟合 δθ(ω)
- 对照需求线 δθ<1.6°,标出 causal 与 refined 各自落点
- 下游闭环:检测 pipeline 分别吃 B2/B3/B4 三种点云,报 recall/RMSE 差

---

## 6. 评估协议

### 6.1 主指标(无需 mocap)

**静态平面厚度,按运动强度分档**(<0.5 / 0.5-2 / >2 rad/s):
- 近场平面拟合后点距分布 std / 95 分位
- 收敛目标 = 数据③实测噪声底线(Mid-360 标称 1σ:≤2cm@10m、≤3cm@0.2m,以自测为准)
- **必须分档**:慢速档所有方法趋同,差异全在快速档;只看均值会稀释

**静止目标自诊断(数据①独有)**:静止无人机自带 GT——
补偿完美时其质心帧间抖动与簇厚度应回到数据③底线;超出部分=残差,
直接量化到厘米。

可选补充:Mean Map Entropy(整图清晰度,无 GT 指标,审稿人友好)。

### 6.2 对照组

| 组 | 配置 | 说明 | 状态 |
|---|---|---|---|
| B1 | FAST-LIO2 | 现有基线 | ❌ |
| B2 | Point-LIO 原生 | 抽稀输出 | ✅ (baseline 跑通) |
| B2' | DLIO | 开源,粗-细两级去畸变,最接近的"精化"类基线 | ❌ |
| B3 | +C1 | 全分辨率因果 | ✅ (`/nearfield/deskewed_*`) |
| B4 | +C2 | 固定滞后平滑精化 | ✅ (`/nearfield/refined_*`,v1) |
| E1 | C2 滞后扫描 L∈{0.1,0.2,0.5,1.0}s | 延迟-精度曲线 | ❌ |
| (可选) | AC-LIO | 官方称将开源;有则加 | ❌ |

延迟:x86(nv-Mercury)与 Jetson 分别测;B4 报"处理延迟+滞后 L"。

### 6.3 下游收益(论文差异化章节)

检测 pipeline(另见 `ICASSP-LiDAR-Drone-Detect`)分别消费 B2/B3/B4 输出,
报 recall@1.0m / RMSE。**"补偿改进→检测收益"证据链他人没有。** ❌ 未开始

---

## 7. 数据分工

| 数据 | 用途 | 状态 |
|---|---|---|
| ③ 全静止(LiDAR+无人机都不动) | 噪声底线标定 | 候选已找到:`1m_front_static_1/2`, `2m_front_static_1` |
| ① 手持晃动 + 静止无人机 | 主战场:先导实验、B2-B4 消融、δθ(ω) 拟合 | 非正式替代数据可用;正式分档版**待录** |
| ② 机载走走停停 | 泛化:真实螺旋桨振动谱 | **待录** |
| ④ MMAUD(地基,有 GT) | 本 README 范围外,归检测侧 | 未确认 |
| mutual_avoidance_uav1.bag | 系统终测 + 下游收益章节 | 存在于 `~/Swarm_LIO2/`,未用于本工作 |

数据①补录规范:静止无人机置于 2-3m 近场内,周围有平面结构;
分档录 静止30s→缓慢1min→快走转身1min→剧烈1min,档间静止3s;
开录先静止 2-3s(IMU 初始化);录 `/livox/lidar` + `/livox/imu`。

---

## 8. 前置检查 —— ✅ 已过

1. per-point `offset_time`:实测每帧跨度 98.99-100.76ms(mean 99.68ms),
   正常,没有"全0/全同"的时间戳丢失特征
2. `/livox/lidar` 实测 10.01Hz(配置 10Hz),`/livox/imu` 实测 200.0Hz
   (Mid360 IMU 规格 200Hz),均一致
3. Point-LIO yaml(Mid-360 实值):
   - `satu_gyro: 35.0`(datasheet 值应为 34.9 = 2000°/s,当前值足够接近,未改)
   - `acc_norm: 1.0` —— 经实测验证:静止时 IMU 加速度模长 mean=0.988,
     确认单位是 g,与当前 `acc_norm: 1.0` 一致
   - `satu_acc: 3.0` —— **发现与实际传感器量程不符**:Mid360 加计量程 ±4g,
     当前设为 3.0g 会把合法的 3-4g 读数错误当作饱和丢弃。**已发现但未修改**
     (用户当场否决了这处 yaml 编辑,保留原值)
   - `extrinsic_T: [-0.011, -0.02329, 0.04412]` —— 未独立核实(无出厂标定书/
     LI-Init 结果可比对,可能已是自行标定值)
   - `extrinsic_est_en: false`、`blind: 0.5` —— 与计划一致,未改动

---

## 9. 实施顺序(原计划,状态已更新)

```
1. 前置检查 + 数据③底线            ✅ 已完成
2. 先导实验(§4)                    ✅ 已完成,结论 GO
3. C1 收尾 + B2 vs B3               ✅ 已完成
4. [若 go] C2 实现 + B4                ✅ 已完成(v1);E1 滞后扫描未做
5. C3 分析                          ❌ 未开始
6. 数据② 泛化 + 下游闭环            ❌ 未开始,依赖新录制
7. (并行)DLIO 基线跑通             ❌ 未开始
```

## 10. 已知坑

- ikd-Tree 是 submodule:`git submodule update --init --recursive`
- Livox-SDK2 + GCC13:加 `#include <cstdint>`
- 浮点参数写 `1000.0`
- 状态流记录量大(~200k/s),二进制 + SSD;子采样后再存也可(已实现 `state_log.hz`)
- 输出写文件不打终端(SSH);可视化 Three.js HTML(Open3D / matplotlib.show() 不可用)
- **本 fork 特有**:`pointlio_mapping` 在 bag 播放结束、输入流断流后会卡进
  `imu/lidar loop back, clear deque/buffer` 的死循环忙等(~80% CPU 持续刷屏),
  不响应 SIGINT/SIGTERM,必须直接 `pkill -9 -f 'lib/point_lio/pointlio_mapping'`。
  用 `ros2 bag record` 录制其输出时如果不第一时间强杀,几分钟内能刷出几个 GB
  垃圾数据。用 `ros2 bag reindex <dir> -s mcap` 可以从被强杀截断的 mcap 恢复
  metadata(`ros2 bag info`/`play` 能正常读,但严格的第三方 mcap 库如 Python
  `rosbags` 会报 "File end magic is invalid",这是良性的)
- Python 分析 PointCloud2 数据时,`sensor_msgs_py.point_cloud2.read_points`
  返回字段是消息声明的原始 dtype(这里是 float32);和大数值(Unix 时间戳)
  相加前必须显式转 float64,否则精度被静默吃掉(见"§4 踩的坑")
- **给任何按时间懒发布(不是逐帧同步发布)的话题做 A/B 对比时**,不要事后
  从两个已录制话题里按"时间最接近"配对消息里的点——批次边界很可能对不上,
  配对到的其实是两个不相关的点(见"C2 实现细节"踩的坑,第一次算出来的
  mean=0.66m 就是这么来的)。正确做法是在产生数据的源头,把需要对比的两个
  值就地存在一起,而不是指望之后从各自发布的话题反推配对

## 11. 论文骨架与归属

```
III. Method
  A. Problem: 估计侧 vs 输出侧去畸变;因果滞后(filtering vs smoothing)
  B. C1 解耦全分辨率补偿
  C. C2 固定滞后平滑二次去畸变(核心,配算法框图)
  D. C3 精度需求分析
IV. Experiments
  A. 因果-平滑差距(先导实验图,motivation)—— 数据已有,待换正式数据①重跑
  B. 平面厚度 × 运动档 × {B1,B2,B2',B3,B4}
  C. 延迟-精度曲线(E1)
  D. 静止目标自诊断(数据①)
  E. 真实飞行泛化(数据②)
  F. 下游检测闭环(独有章节)
V. Limitation: 不提升 ATE(不反馈配准);目标运动畸变另文处理
```

叙事线(related work 结尾):帧级一次(FAST-LIO2)→ 帧级两级(DLIO)
→ 帧级迭代(AC-LIO/ADC-LIO)→ 连续时间(CT 家族)→ 逐点因果(Point-LIO)
→ **逐点平滑·输出侧(本工作)**。

归属备忘:
- 思想来源致谢:AC-LIO/ADC-LIO(平滑式二次去畸变,估计侧先例)、
  Point-LIO(逐点状态框架)、Vibration-aware LIO(残差存在性论证)
- 本工作:输出侧定位 + 全分辨率解耦 + 固定滞后双档输出 + 下游闭环评估

审稿人预答:
- Q"和 AC-LIO 区别?" → 他们帧级、估计回路内、服务位姿;我们逐点、
  回路外、服务输出;且我们有下游检测证据链,他们只有 ATE
- Q"为什么不反馈进配准提升 ATE?" → 有意设计:零估计器风险 + 可整体
  开关;反馈版列为 future work
- Q"平滑延迟不可接受?" → 双档输出,延迟敏感任务用 causal 档;
  E1 给出延迟-精度曲线供选择
- Q"为什么不用连续时间方法?" → 引 AC-LIO 对 CT 的批评(高速下模型
  失配 + 计算代价);我们在 Jetson 上给出实测延迟
